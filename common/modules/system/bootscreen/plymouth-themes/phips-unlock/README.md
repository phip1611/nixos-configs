# Phips Unlock Plymouth theme

A black Plymouth theme with the configured Phips logo centered on screen.

- Normal boot shows a white Orbit spinner below the logo.
- Disk unlock shows a compact `Unlock Disk` password prompt without device
  identifiers.
- After the prompt closes, the normal boot spinner returns.

The configured NixOS Plymouth logo and font are used by the theme.
